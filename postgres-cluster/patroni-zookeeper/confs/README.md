# confs

The two files that differ from `../../patroni_etcd/manual/`:
`zoo.cfg` for the ZooKeeper ensemble, and `patroni_dcs_snippet.yml`,
the replacement for the `etcd:` block in `patroni.yml`. See
`../README.md` for how to use them.
